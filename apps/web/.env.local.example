# Copy to apps/web/.env.local (gitignored) — or just merge these two lines
# into your existing .env.local if Vercel's CLI has already created one with
# a VERCEL_OIDC_TOKEN in it. These are Supabase's well-known local dev
# defaults (same for every `supabase start`), not secrets:
PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH
